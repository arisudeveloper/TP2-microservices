apiVersion: v1
kind: Service
metadata:
  name: container-livro
spec:
  type: NodePort
  selector:
    app: servicelivro
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080cd k