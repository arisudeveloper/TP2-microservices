apiVersion: v1
kind: Service
metadata:
  name: container-emprestimo
spec:
  type: NodePort
  selector:
    app: serviceemprestimo
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081